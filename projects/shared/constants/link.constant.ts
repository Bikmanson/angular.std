export const BASE_URL = 'http://localhost';
export const DB_PORT = 3000;
export const GET_USERS = `${BASE_URL}:${DB_PORT}/users`;
export const GET_ROLES = `${BASE_URL}:${DB_PORT}/roles`;
export const GET_TASKS = `${BASE_URL}:${DB_PORT}/tasks`;
export const TASKS_ENDPOINT = `${BASE_URL}:${DB_PORT}/tasks`;
export const USERS_ENDPOINT = `${BASE_URL}:${DB_PORT}/users`;
export const ROLES_ENDPOINT = `${BASE_URL}:${DB_PORT}/roles`;
